export const NAVIGATION_BAR_HEIGHT_PX = 80
export const TABS_HEIGHT_PX = 50
export const EMBED_HEADER_HEIGHT_PX = 56
// Total vertical space the bottom-sheet drag handle occupies (margin 10 +
// pill 5 + margin 10). In the peek, the drag-handle band is the analogue of
// the full page's tab ribbon (TABS_HEIGHT_PX), so the whitespace placeholder
// below the handle grows to make (handle + placeholder) === TABS_HEIGHT_PX.
export const DRAG_HANDLE_HEIGHT_PX = 25
